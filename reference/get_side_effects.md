# Extract side effect data

Extract side effect data

## Usage

``` r
get_side_effects(export)
```

## Arguments

- export:

  A data frame containing the raw REDCap export.

## Value

A data frame of side effect data.
