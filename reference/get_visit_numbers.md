# Get visit numbers

Get visit numbers

## Usage

``` r
get_visit_numbers(df)
```

## Arguments

- df:

  A data frame from one REDCap tab.

## Value

The \`visit_number\` column, or NA for each row if absent.
