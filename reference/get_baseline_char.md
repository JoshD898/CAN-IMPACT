# Extract baseline characteristics data

Extract baseline characteristics data

## Usage

``` r
get_baseline_char(export)
```

## Arguments

- export:

  A data frame containing the raw REDCap export.

## Value

A data frame of baseline characteristics data (everything not included
in follow up visits)
