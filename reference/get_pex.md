# Extract pulmonary exacerbation data

Extract pulmonary exacerbation data

## Usage

``` r
get_pex(export)
```

## Arguments

- export:

  A data frame containing the raw REDCap export.

## Value

A data frame of pulmonary exacerbation data.
