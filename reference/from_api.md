# USe the REDCap API to load a raw export

USe the REDCap API to load a raw export

## Usage

``` r
from_api(token)
```

## Arguments

- token:

  API token to use

## Value

A data frame of the raw export data
