# Extract visit data

Extract visit data

## Usage

``` r
get_visits(export)
```

## Arguments

- export:

  A data frame containing the raw REDCap export.

## Value

A data frame of visit data.

## See also

\[stack_visits()\]
