pregnancy_labels <- list(
  preg_result = list(
    fields = "preg_result",
    values = c(
      `1` = "spontaneous abortion",
      `2` = "therapeutic abortion",
      `3` = "live birth"
    )
  )
)
